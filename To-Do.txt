3.0 New Features:

- 64 bits !
- Growl
- Magnetic Windows
- Automatically eject CD/DVD after importing
- Better support for 4D series

TODO:

- HUD Windows
- Edit DICOM fields
- Global Prefs 3D (thumbnails, shading, CLUT, opacity, ...)
- Editable image annotation options
- Local database in the 'Sources' area
- New study for keyimages
- Perfusion
- Registration (vtkLandmark, itkLandmark)
- ROI enhancement
- Worklist DICOM
- Plugins Manager
- Windows state (DB study)
- Virtual matrix for Windows position

DB:

Study Level:
- Locked
- Remove 'type' field
- Windows state (BLOB, windows position + series ID)

Series Level:
- Remove 'type' field
- Add 'noOfKeyimages' field

Image Level:
- Remove 'name' field
- Remove 'type' field
- Set fileType to 0L for DICOM file
- Compress sopInstanceUID: (sopInstanceUIDEncode, sopInstanceUIDDecode)
- Create sopInstanceUID with Numbers and . ONLY, including for non-DICOM objects
- PathNumber : 64 bit Int: if no path, create the path with PathNumber + '.' + extension
- InstanceNumber : 32 bits, instead of 64 bits
- numberOfFrames: if not available, return 1
- Remove 'frameNo' field
- Add 'frameID' field
- numberOfSeries: if not available, return 1
- Add optional 'wl' & 'ww' & 'zoom' & 'offset' field

DICOM SR:
- Referenced UID
